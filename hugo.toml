# Base URL - will be overridden by Vercel environment variable
baseURL = 'http://localhost:1313/'
languageCode = 'en-us'
title = 'Modelyo - Confidential Cloud'

# Disable taxonomies to remove warning (using correct syntax)
disableKinds = ['taxonomy']

[params]
  description = "Modelyo - The Confidential Cloud"
